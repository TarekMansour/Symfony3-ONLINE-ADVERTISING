{% extends 'base.html.twig' %}
{% block title %}Book edition{% endblock %}
{% block body %}
    <div class="row">
            <ol class="breadcrumb">
                <li><a href="{{ path('annonce_books_dashboard')}}"><svg class="glyph stroked home"><use xlink:href="#stroked-home"></use></svg></a></li>
                <li class="active">books edition section</li>
            </ol>
        </div><!--/.row-->

    <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header"> books edition</h1>
            </div>
        </div><!--/.row-->

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <div class="form-group"> 
        <div class="col-sm-offset-2 col-sm-10">
        <input type="submit" value="Edit"  class="btn btn-info" />
        </div>
        </div>
    {{ form_end(edit_form) }}
    <div class="form-group"> 
    <div class="col-sm-offset-2 col-sm-10">
    <ul>
        <li>
            <a href="{{ path('annonce_books_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
    </div>
    </div>
{% endblock %}
